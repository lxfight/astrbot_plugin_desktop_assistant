# 桌面悬浮球助手 - Python 依赖
# ================================

# GUI 框架
PySide6>=6.5.0,<7.0.0                  # Qt6 官方 Python 绑定

# 屏幕截图
mss>=9.0.0                              # 跨平台高性能屏幕截图

# 图像处理
Pillow>=10.0.0                          # 图像处理库

# 语音处理
sounddevice>=0.4.6                      # 跨平台音频录制
soundfile>=0.12.0                       # 音频文件读写（依赖 libsndfile）

# 可选：语音播放（备选方案）
# pygame>=2.5.0                         # 如果 sounddevice 播放有问题可启用

# 系统工具
psutil>=5.9.0                           # 进程和系统监控（内存管理）

# 注意事项：
# 1. PySide6 在某些 Linux 发行版可能需要额外安装系统依赖
#    Ubuntu/Debian: sudo apt install libxcb-xinerama0 libxcb-cursor0
# 2. soundfile 依赖 libsndfile
#    Ubuntu/Debian: sudo apt install libsndfile1
#    macOS: brew install libsndfile
#    Windows: 自动包含
# 3. 如果在 Wayland 环境下截图有问题，可能需要额外配置